{
  "name": "anvaya-monorepo",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend",
    "vercel-build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "supabase": "^2.115.0"
  }
}
